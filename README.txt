Blockscape Server
========================
This is a server software for Blockscape.

Node.js must be installed in order for the server to work.

Starting the server
========================
Use this on a command prompt:
    npm install

This will install anything the server requires.
If this command is done, Type this:
    node .
